{
  "name": "clawd-agent-launcher",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:api": "cd packages/api && npm run dev",
    "dev:frontend": "cd packages/frontend && npm run dev",
    "build": "npm run build:api && npm run build:frontend && npm run build:cli && npm run build:sdk",
    "build:api": "cd packages/api && npm run build",
    "build:frontend": "cd packages/frontend && npm run build",
    "build:cli": "cd packages/cli && npm run build",
    "build:sdk": "cd packages/x402-sdk && npm run build",
    "typecheck": "tsc --noEmit -p packages/api/tsconfig.json && tsc --noEmit -p packages/cli/tsconfig.json && tsc --noEmit -p packages/x402-sdk/tsconfig.json"
  },
  "engines": {
    "node": ">=18"
  }
}
